# Schema
The schema defines the core data contract and determines what is valid data. The schema is split into that of the entire `workspace` (the project graph and the workspace metadata), the working project `graph` (folder, blocks, relations, and views defined in the current project), and the `action surface` that supports the project log and action history.

## Workspace Schema
## Graph Schema
## Action Schema
