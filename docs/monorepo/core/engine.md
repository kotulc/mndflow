# Engine
The core collection of modules that define the graph and action surfaces, and the rules and interfaces between them.

## Engine Modules
The log, the fold, the door, containment, rules, ids, references, placement, routing, the action set — the whole closed engine is composed of modules that determine how each block, relation, or view behaves when the user interacts with them.
