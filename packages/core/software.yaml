# Relationship definitions a software project starts with.
# Editable in the app after they land; this is only where a new project begins.

definitions:
  - name: depends on
    form: line
  - name: calls
    form: line
  - name: reads from
    form: line
  - name: writes to
    form: line
  - name: extends
    form: line
