# packages/requirements

A requirement is a block with `id` and `text`, related by five directed
definitions. Data only — no module, no renderer, no layout law.

Loading into a project is A0.3. Until then these files sit ready; nothing
imports them. Card `shows` is configured here; the canvas still draws from
its hard-wired card until S2.6.
