# packages/parametrics

A constraint is a block with a declared size and a style set. Data only —
no module, no renderer, no layout law.

Loading into a project is A0.3. Until then these files sit ready; nothing
imports them. Style draws once S2.6 reads the set; layout reading `size`
for a block is still the ordinary leaf until that wiring lands.
