/** The SysML style set — the name the spec uses as its example.
 *
 *  Look values land with the packages that draw from it; the set existing is
 *  what lets a definition name it and the door accept the name. */

export const sysml = { name: "sysml" as const };
